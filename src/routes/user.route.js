// const userRoute = require("express").Router();
// const controller = require("../controllers/auth.controller");
// const { wrapper } = require("../utils/index.js");

// userRoute.get("/user", wrapper(controller.getUser));
